// GENERATED BY UNITY. REMOVE THIS COMMENT TO PREVENT OVERWRITING WHEN EXPORTING AGAIN
buildscript {
	repositories {
		jcenter()
		mavenCentral() // add repository
        maven {
            url "https://maven.google.com"
        }
	}

	dependencies {
		// classpath 'com.android.tools.build:gradle:2.1.2'
		classpath 'com.android.tools.build:gradle:2.3.0'
		// classpath 'com.android.tools.build:gradle:3.0.1'
		classpath 'com.google.gms:google-services:3.2.0'
	}
}

allprojects {
   repositories {
		flatDir {
			dirs 'libs'
		}
		jcenter()

		maven {
			url "https://maven.google.com" // Google's Maven repository
		}
		maven {
			//url "http://central.maven.org/maven2"
			url 'https://s3.ap-northeast-2.amazonaws.com/kakao-sdk-release/release/'
			name 'Kakaogames'
		}
		maven {
			url "http://repo.spring.io/plugins-release/"
		}
   }

    ext {
        android_compile_version = **APIVERSION**

        // Google Play Services minimum requirements is 16
        android_min_sdk_version = 16
    }
}

apply plugin: 'com.android.application'

dependencies {
	compile fileTree(dir: 'libs', include: ['*.jar'])

	compile 'com.android.support:multidex:1.0.1'
	compile "com.kakaogame:idp_device:3.8.1"
    compile "com.kakaogame:idp_facebook:3.8.1"
    compile "com.kakaogame:idp_googlegame:3.8.1"
    compile "com.kakaogame:idp_kakao:3.8.1"
    compile "com.kakaogame:gamesdk:3.8.1"
    compile "com.android.billingclient:billing:1.1"
	
**DEPS**}

android {
	compileSdkVersion android_compile_version
	buildToolsVersion '**BUILDTOOLS**'

	defaultConfig {
		minSdkVersion android_min_sdk_version
		targetSdkVersion **TARGETSDKVERSION**
		applicationId '**APPLICATIONID**'
		multiDexEnabled true
	}

	dexOptions {
		preDexLibraries false
		javaMaxHeapSize "4g"
	}

	lintOptions {
		abortOnError false
	}

	aaptOptions {
		noCompress '.unity3d', '.ress', '.resource', '.obb'**STREAMING_ASSETS**
	}

**SIGN**
	buildTypes {
  		debug {
			minifyEnabled true
			useProguard true
  			proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt', 'NTModule/proguard.txt' **USER_PROGUARD**
  			jniDebuggable true
  		}
  		release {
 			minifyEnabled true
 			useProguard true
  			proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt', 'NTModule/proguard.txt' **USER_PROGUARD**
  			**SIGNCONFIG**
  		}
	}
}
